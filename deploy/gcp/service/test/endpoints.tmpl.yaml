type: google.api.Service
config_version: 3

name: svctest.endpoints.${project_id}.cloud.goog

title: Service test gRPC API
apis:
- name: svctest.Hello

usage:
  rules:
  - selector: "*"
    allow_unregistered_calls: true
